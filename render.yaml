services:
  - type: web
    name: production-project-v7-web
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run start:prod
  - type: worker
    name: production-project-v7-worker
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run worker
